import { UrgencyDirective } from './urgency.directive';

describe('UrgencyDirective', () => {
  it('should create an instance', () => {
    const directive = new UrgencyDirective();
    expect(directive).toBeTruthy();
  });
});
