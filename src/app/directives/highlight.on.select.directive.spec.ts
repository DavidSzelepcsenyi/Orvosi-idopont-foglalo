import { HighlightOnSelectDirective } from './highlight.on.select.directive';

describe('HighlightOnSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightOnSelectDirective();
    expect(directive).toBeTruthy();
  });
});
